<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash/window background matches the ORDnet beige; the real theming is Compose. -->
    <style name="Theme.Ordplug" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/ordnet_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
